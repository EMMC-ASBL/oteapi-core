image: docker:20.10.8
  
stages:
  - build

services:
    - docker:20.10.8-dind

variables:
  IMAGE: $CI_REGISTRY_IMAGE


docker-build:
  stage: build
  tags:
    - docker

  before_script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    
  script:
    - docker build --target production --tag $IMAGE:$CI_COMMIT_SHA --t $IMAGE:latest .
    - docker push $IMAGE:$CI_COMMIT_SHA
    - docker push $IMAGE:latest

  only:
    - master
    - merge_request
